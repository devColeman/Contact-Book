<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact List</title>
    <link rel="stylesheet" href="css/listStyles.css">
</head>
<body>
    <a href="/">Home</a>
    <a href="/add-contact">Add a Contact</a>
    <h1>contact list</h1>
    <% for(let i = 0; i < contacts.length; i++) {%>
        <div>
            <% if(contacts[i].favorite == true) {%>
                <p>⭐</p>
            <%} %>
            add a reload so the star gets add on maybe add margin so the item doesnt take make more space
            <p class="<%= contacts[i]._id %>"><%= contacts[i].name %></p>
            <p><%= contacts[i].phoneNumber %></p>
            <p><%= contacts[i].email %></p>
            <button class="favoriteIcon"  >Favorite</button>
            <button class="unFavoriteIcon"  href="#">Unfavorite</button>

        </div>
     <% } %>
     
     <script src="js/main.js"></script>
</body>
</html>

<!-- add like contacts and remove then add edit feature -->